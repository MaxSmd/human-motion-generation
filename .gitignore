__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

.venv/
.env
.envrc

.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage

# backend rendered-media cache (server/cache.py)
.media/
# durable backend state (job registry + active-model flag) — docker bind mount
.appstate/

# working notes / slide sources — kept locally, not part of the submission
.docs/
.presentation/

# experiment outputs
runs/
outputs/
work/
wandb/
lightning_logs/
checkpoints/
*.ckpt
*.pt
!tests/**/*.pt

# generated frontend / artifact dependencies
node_modules/

# data (anchored: only the downloaded blob dirs under external/, NOT src/rmg/data/)
/external/data/
/external/momask-codes-official/
/data/
*.zip
*.tar
*.tar.gz
*.npz
*.h5
*.sqsh

# stray logs + LaTeX aux junk (never commit these; runs/ already covers job output)
*.log
*.aux
*.fls
*.fdb_latexmk
*.synctex.gz

# editor / OS
.vscode/
.idea/
.DS_Store
