# RMG container Python deps. Layered on top of pytorch/pytorch:2.9.0-cuda13.0-cudnn9-devel.

# Modeling / training
transformers>=4.46
accelerate>=0.34
roma>=1.5.0
einops>=0.8

# Config / orchestration
hydra-core>=1.3
omegaconf>=2.3

# Logging
wandb>=0.18
tensorboard>=2.18

# Data / IO
h5py>=3.11
scipy>=1.13
tqdm>=4.66
matplotlib>=3.9
huggingface_hub>=0.25

# AMASS preprocess (only used by scripts/prepare_humanml3d.py)
human_body_prior @ git+https://github.com/nghorbani/human_body_prior.git
body_visualizer @ git+https://github.com/nghorbani/body_visualizer.git ; sys_platform != 'darwin'
chumpy

# Dev / testing
pytest>=8
pytest-xdist>=3
ruff
