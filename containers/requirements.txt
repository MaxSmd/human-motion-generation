# RMG container Python deps. Layered on top of pytorch/pytorch:2.9.0-cuda13.0-cudnn9-devel.
#
# Two things to preserve when changing this file:
#  1. Do NOT add `torch` here — the base image already ships a CUDA-built torch
#     and pulling another version from PyPI will downgrade to a CPU-only build.
#     The build sbatch keeps torch frozen via `pip install --no-deps` for any
#     package that would otherwise resolve a different torch.
#  2. Cap `transformers<5` — at the time of writing a 5.x dev version exists on
#     PyPI which pulls a torch downgrade chain. 4.x covers everything we need
#     (Qwen3-Embedding-0.6B included).

# Modeling / training
transformers>=4.46,<5
accelerate>=0.34
roma>=1.5.0
einops>=0.8
smplx>=0.1.28
# MARDM's SiT diffusion head uses an ODE sampler from torchdiffeq. Its only
# deps are torch + scipy (both already present), so it won't downgrade torch.
torchdiffeq>=0.2.4

# Config / orchestration
hydra-core>=1.3
omegaconf>=2.3

# Logging
wandb>=0.18
tensorboard>=2.18

# Data / IO
h5py>=3.11
scipy>=1.13
tqdm>=4.66
matplotlib>=3.9
huggingface_hub>=0.25

# Eval text preprocessing (Guo et al. evaluator → spaCy POS tagging)
spacy>=3.7,<4

# Dev / testing
pytest>=8
pytest-xdist>=3
ruff
