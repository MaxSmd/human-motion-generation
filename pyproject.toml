[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "motion-reproductions"
version = "0.0.1"
description = "Shared codebase for RMG, MoMask, and MARDM reproductions on HumanML3D"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.4",
    "numpy",
    "einops",
    "scipy",
    "h5py",
    "tqdm",
    "matplotlib",
    "hydra-core",
    "omegaconf",
    "transformers",
    "accelerate",
    "huggingface_hub",
    "wandb",
    "torchdiffeq",
]

[project.optional-dependencies]
dev = ["pytest", "pytest-xdist", "ruff", "mypy"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["shared*", "rmg*", "momask*", "mardm*", "projflow*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 110
target-version = "py310"
