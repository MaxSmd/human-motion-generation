"""Host-side analysis: cheap work on small artifacts pulled from the cluster.

Right now: eval-metric parsing + run-comparison tables (incl. a copy-ready LaTeX
booktabs tabular). `.npy` trajectory/jitter plots are a planned follow-up.
"""
